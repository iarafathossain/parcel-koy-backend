model Category {
    id         String @id @default(uuid(7)) @db.Uuid
    name       String
    slug       String @unique
    baseWeight Float

    parcels Parcel[]
    pricing Pricing[]

    @@map("categories")
}
