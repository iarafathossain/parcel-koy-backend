enum Gender {
    MALE
    FEMALE
    OTHER
}

enum Role {
    MERCHANT
    RIDER
    ADMIN
    SUPER_ADMIN
}

enum ParcelStatus {
    // --- Phase 1: Creation & Pickup ---
    REQUESTED
    PICKUP_RIDER_ASSIGNED
    PICKED_UP
    PICKUP_FAILED

    // --- Phase 2: Hub & Transit ---
    RECEIVED_AT_ORIGIN_HUB
    IN_TRANSIT
    RECEIVED_AT_DESTINATION_HUB

    // --- Phase 3: Delivery ---
    OUT_FOR_DELIVERY
    DELIVERED
    PARTIAL_DELIVERY
    DELIVERY_FAILED

    // --- Phase 4: Exceptions & Returns ---
    ON_HOLD
    RETURNED_TO_MERCHANT
    CANCELLED
}

enum UserStatus {
    ACTIVE
    BLOCKED
    DELETED
}

enum PayoutStatus {
    PENDING
    COMPLETED
    REJECTED
}

enum MethodType {
    PICKUP
    DELIVERY
}

enum PaymentProviderType {
    BANK
    BKASH
    NAGAD
    ROCKET
}
